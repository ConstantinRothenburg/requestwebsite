# Request Website v2.0

This repo is a container. The active app lives in `prismic-next/`.

Quick start:

```sh
cd prismic-next
npm install
npm run dev
```

Docs:
- Prismic setup: prismic-next/docs/llm/REQUEST_SETUP.toon
- Content guide: prismic-next/docs/llm/REQUEST_CONTENT_GUIDE.toon
